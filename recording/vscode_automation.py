def open_vscode(filepath: str) -> None:
    # opens VS Code with the given file path
    pass

def type_code(code: str, delay_per_char: float = 0.05) -> None:
    # types code into the active VS Code window at readable speed
    pass
